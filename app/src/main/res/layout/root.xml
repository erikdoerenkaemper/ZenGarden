<androidx.constraintlayout.widget.ConstraintLayout android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:id="@+id/root"
    android:background="@drawable/hintergrund1"
    android:clipChildren="false"
    android:clipToPadding="false"
    xmlns:android="http://schemas.android.com/apk/res/android">


    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:id="@+id/buttons"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:padding="20dp">



        <Button
            android:id="@+id/wasser_button"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:foreground="@drawable/giesskanne"/>

        <Button
            android:id="@+id/duenger_button"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:foreground="@drawable/duenger"/>

        <Button
            android:id="@+id/samen_button"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:foreground="@drawable/samen_pack"/>

        <Button
            android:id="@+id/verkaufen_button"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:foreground="@drawable/verkaufen"
            android:textSize="20sp"/>

        <Button
            android:id="@+id/verschieben_button"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:foreground="@drawable/verschieben"/>
    </LinearLayout>

    <GridLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:id="@+id/gartengrid"
        android:rowCount="3"
        android:columnCount="6"
        app:layout_constraintStart_toEndOf="@id/buttons"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:clipChildren="false"
        android:clipToPadding="false">
    </GridLayout>

    <TextView
        android:id="@+id/geld_anzeige"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/geld_background"
        android:gravity="center"
        android:minWidth="120dp"
        android:padding="12dp"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:textColor="@color/geld_gruen"
        android:textSize="24sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />


</androidx.constraintlayout.widget.ConstraintLayout>
